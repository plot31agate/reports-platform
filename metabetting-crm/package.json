{
  "name": "df-metabetting-crm",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Meta Betting CRM Snapshot: turns what we find in Meta Betting's Customer.io data into a funnel, legal audience, segment build plan, Swifty data requests, opportunity estimate and roadmap. Counts only, held in the browser (localStorage + JSON export). Served at /metabetting-crm by the reporting app, same DF portal style as Agency HQ.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
